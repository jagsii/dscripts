exports=async function(url,bid,cid,options){const clientOptions={...(options&&options.clientOptions),encoding:"utf-8","x-mobile":0},{send}=require("http");let response=await send(url,clientOptions);assertOK(response);let html=response.body;const iframe=html.subRegex(/<iframe [^>]*src="([^"]+)"/);return iframe?(response=await send({url:iframe.replace(/&amp;/g,"&"),headers:{referer:url}}),assertOK(response),html=response.body,{url:html.subRegex(/<source [^>]*src="([^"]+)"/).trim()}):null};