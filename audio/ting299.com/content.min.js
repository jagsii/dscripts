exports=async function(url,bid,cid,options){const clientOptions={...(options&&options.clientOptions)};stat("request");const{send}=require("http");let response=await send(url,clientOptions);stat("request"),assertOK(response);let html=response.body;return {url:html.subRegex(/audio[^>]+src="([^"]+)"/)}};