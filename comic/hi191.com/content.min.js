exports=async function(url,bid,cid,options){const{send}=require("http"),clientOptions={...(options&&options.clientOptions)};stat("request");let response=await send(url,clientOptions);stat("request"),assertOK(response);const html=response.body,fragment=html.substring(html.indexOf("class=\"comicpage\""),html.lastIndexOf("class=\"fanye\"")),URL=require("url"),uri=new URL(url),urls=fragment.subRegexAll(/data-original="([^"]+)"/g).map(_=>uri.resolve(_));return {urls}};