exports=async function(url,bid,cid,options){const clientOptions={...(options&&options.clientOptions),encoding:"utf-8","x-mobile":0};stat("request");const{send}=require("http");let response=await send(url,clientOptions);stat("request"),assertOK(response);let html=response.body;const iframeUrl=html.subRegex(/<iframe id="play" name="play" src="([^"]+)" id="xplayer"/);if(!iframeUrl)return null;const absoluteUrl=require("utils/absolute_url");response=await send({url:absoluteUrl(iframeUrl,url),headers:{Referer:url}}),assertOK(response),html=response.body;const{expr,mp3}=html.subRegexMap(/<script>(?<expr>[^(]+?)function next[\S\s]+?jPlayer\("setMedia", {\s*mp3:(?<mp3>[^}\n]+)/);if(!expr||!mp3)return null;const _=eval(`${expr};
  ${mp3}`);return {url:_,headers:{Referer:"https://m.2uxs.com/"}}};