function stepOverString(pos,html){const q=html.charAt(pos);++pos;for(let c;pos<html.length;){if(c=html.charAt(pos),"\\"===c){pos+=2;continue}if(html.charAt(pos)===q)return pos;pos++;}return -1}function getScriptContent(pos,html){const begin=pos;for(let c;pos<html.length;){if(c=html.charAt(pos),"'"===c||"\""===c){if(pos=stepOverString(pos,html),-1===pos)return html.substring(begin);pos++;continue}if("<"===c&&pos+7<html.length&&"/script"===html.substr(pos+1,7).toLowerCase()){const endPos=pos;for(pos+=8;pos<html.length;){if(">"===html.charAt(pos))return html.substring(begin,endPos);if(" "===html.charAt(pos)){pos++;continue}break}}pos++;}return html.substring(begin)}function getCommentTag(pos,html){if(pos+3>=html.length)return null;if("!"!==html.charAt(pos+1)||"-"!==html.charAt(pos+2)||"-"!==html.charAt(pos+3))return null;const name="#comment",open=!0,begin=pos;pos+=4;for(let c;pos<html.length;){if(c=html.charAt(pos),"-"===c&&pos+2<html.length&&"-"===html.charAt(pos+1)&&">"===html.charAt(pos+2))return {name,open,begin,end:pos+3,selfClosing:!0,extra:html.substring(begin+4,pos)};pos++;}return {name,open,begin,end:html.length,extra:html.substring(begin+4)}}function getTextTag(pos,html){const begin=pos,open=!0,name="#text";for(pos++;pos<html.length;){if("<"===html.charAt(pos))return {name,open,begin,end:pos,extra:html.substring(begin,pos)};pos++;}return {name,open,begin,end:html.length,extra:html.substring(begin,html.length)}}function isTagChar(char){return 48<=char&&57>=char||97<=char&&122>=char||65<=char&&90>=char||45===char||58===char}function getTagName(pos,html){const begin=pos;for(;pos<html.length&&!!isTagChar(html.charCodeAt(pos));)pos++;return pos===begin?null:html.substring(begin,pos)}function getOpeningTag(pos,html){const name=getTagName(pos+1,html);if(!name)return null;const open=!0,begin=pos;pos=pos+name.length+1;const attrBegin=pos;for(let c;pos<html.length;){if(c=html.charAt(pos),"'"===c||"\""===c){if(pos=stepOverString(pos,html),-1===pos)return {name,open,attributes:html.substring(attrBegin,html.length),begin,end:html.length};pos++;continue}if("<"===c)return {name,open,attributes:html.substring(attrBegin,pos),begin,end:pos};if(">"===c){const selfClosing="/"===html.charAt(pos-1);return {name,open,attributes:html.substring(attrBegin,selfClosing?pos-1:pos),begin,end:pos+1,selfClosing}}pos++;}return {name,open,attributes:html.substring(attrBegin,html.length),begin,end:html.length}}function getClosingTag(pos,html){if(pos+1>=html.length||"/"!==html.charAt(pos+1))return null;const name=getTagName(pos+2,html);if(!name)return null;const begin=pos,open=!1;for(pos+=2+name.length;pos<html.length;){if(">"===html.charAt(pos))return {name,open,begin,end:pos+1};if("<"===html.charAt(pos))return {name,open,begin,end:pos};pos++;}return {name,open,begin,end:html.length}}function htmlTokenize(html,options){options={filterEmptyString:!1,trimString:!1,trimAttributes:!0,lowerCaseTagName:!0,...options};const result=[];let pos=0;const length=html.length;for(let c;pos<length;){c=html.charAt(pos);let tag;if("<"===c&&(tag=getCommentTag(pos,html)||getOpeningTag(pos,html)||getClosingTag(pos,html),tag)){options.lowerCaseTagName&&(tag.name=tag.name.toLowerCase()),pos=tag.end,tag.attributes&&options.trimAttributes&&(tag.attributes=tag.attributes.trim()),void 0!==tag.attributes&&0===tag.attributes.length&&delete tag.attributes,"script"===tag.name.toLowerCase()&&tag.open&&(tag.extra=getScriptContent(pos,html),tag.extra&&(pos+=tag.extra.length)),result.push(tag);continue}const textTag=getTextTag(pos,html);options.trimString&&(textTag.extra=textTag.extra.trim()),(!options.filterEmptyString||0<textTag.extra.length)&&result.push(textTag),pos=textTag.end;}return result}exports=htmlTokenize;