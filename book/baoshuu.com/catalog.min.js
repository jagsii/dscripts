exports=async function(bid,options){const url=`https://m.baoshuu.com/TXT/down_${bid}.html`,{send}=require("http"),URL=require("url"),clientOptions={...(options&&options.clientOptions),encoding:"gbk","x-mobile":1};let response=await send(url,clientOptions);assertOK(response);let html=response.body;const u=html.subRegex(/href="([^"]+)"[^>]*>在线阅读/);if(!u)return [];const uri=new URL(url),encodeUriGBK=require("utils/encode_uri_gbk"),uu=uri.resolve(encodeUriGBK(u));response=await send(uu,clientOptions),assertOK(response),html=response.body;const totalStr=html.subRegex(/当前是第:(?:\s|&nbsp;)*\d+(?:\s|&nbsp;)*\/(?:\s|&nbsp;)*(\d+)(?:\s|&nbsp;)*页/),total=parseInt(totalStr)+1,result=[],base=uu.replace(/&yeshu=\d+/,"");for(let i=0;i<total;i++){const id=i+"";result.push({id,name:id,url:`${base}&yeshu=${i}`});}return formatCatalog(result)};