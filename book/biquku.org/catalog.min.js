exports=async function(bid,options){const{parseCatalog}=await require("transcode/parse_catalog"),id=-1===bid.indexOf("/")?`${bid.idPrefix()}/${bid}`:`book/${bid}`;return parseCatalog(`https://www.biquku.org/${id}/`,".section-list",{clientOptions:{...(options&&options.clientOptions)}})};